# AI-Powered Manim Text-to-Video Pipeline Requirements
# =========================================================

# Core Manim library for video generation
manim==0.17.3

# OpenAI API for AI code generation
openai==1.3.0

# Standard Python libraries (usually pre-installed)
# These are listed for documentation but often come with Python:
# - re (regex)
# - subprocess (system commands)
# - json (JSON handling)
# - os (operating system)
# - ast (abstract syntax trees)
# - dataclasses (Python 3.7+)
# - typing (type hints)

# Optional but recommended for development
ipython>=8.0.0  # Better Python REPL
jupyter>=1.0.0  # Notebook support
